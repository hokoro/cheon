/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
        "./src/**/*.{js,jsx,ts,tsx}", // Tailwind가 적용될 파일 경로 설정
    ],
    theme: {
        extend: {}, // 커스터마이징할 테마 추가 가능
    },
    plugins: [],
};
